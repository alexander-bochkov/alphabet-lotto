import React from 'react';
import {RandomLetterControls} from '../../library';

export default {title: 'RandomLetterControls'};

export const common = () => (
    <RandomLetterControls
        onNextLetterClick={() => {}}
        onResetClick={() => {}}
    />
);
