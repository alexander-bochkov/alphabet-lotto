import React from 'react';
import {composeClassName} from '../../../utils';

export const Title = () => (
    <div className={composeClassName('game-cards__title')}>
        Alphabet Lotto
    </div>
);
